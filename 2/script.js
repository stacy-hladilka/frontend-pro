// the first part
const userName = prompt("What is your name?");
const userAge = prompt("How old are you?");
const userLocation = prompt("What is your current location?");
alert(
  `Hello, ${userName}, I possess the information that you are ${userAge}, and this is your exact current location: ${userLocation}.`
);
// the second part
const randomNumber = 53678;
const randomString = String(randomNumber);
alert(
  `${randomString[2]} ${randomString[0]} ${randomString[3]} ${randomString[1]} ${randomString[4]}`
);
